library(dslabs)
data(heights)
options(digits = 3)    # report 3 significant digits for all answers
head(heights)
npessoa <- heights$height
length(npessoa)
nrow(heights)
avg <- mean(heights$height)
avg
line <- filter(heights, heights$height>avg)
nrow(line)
q2 <- filter(heights, heights$height>avg & heights$sex == "Female")
nrow(q2)
q3 <- filter(heights, heights$sex == "Female")
nrow(q3)
238/length(npessoa)

filter(heights, heights$height==50)
index <- match(50, heights$height)
max(heights$height)
min(heights$height)
x <- 50:82
 
match(50, heights$height)
heights[1032, ]

x2 <- x %in% heights$height
which(x2)
which(!x2)
sum(!x2)
library(dplyr)
heights2 <- mutate(heights, ht_cm = heights$height*2.54)
head(heights2)
heights2[18, ]
meancm <- mean(heights2$ht_cm)
meancm

female <- filter(heights2, sex == "Female")
nrow(female)
avgfemalecm <- mean(female$ht_cm)
avgfemalecm

#QUESTION8
library(dslabs)
data(olive)
head(olive)
#percent palmitic acid versus palmitoleic acid in a scatterplot
plot(olive$palmitic, olive$palmitoleic)
#Create a histogram of the percentage of eicosenoic acid in olive.
hist(olive$eicosenoic)
#Make a boxplot of palmitic acid percentage in olive with separate distributions for each region.
boxplot(palmitic~region, data = olive)
